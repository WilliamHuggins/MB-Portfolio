# No runtime secrets are required — the site is fully static.
# Set this only if you deploy to a custom domain and want the canonical
# URLs in sitemap.xml and the page <head> to match.
SITE_URL="https://marielabrown.com"
